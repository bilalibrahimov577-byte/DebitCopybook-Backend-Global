spring:
  application:
    name: DebtCopybook
  datasource:
    url: jdbc:postgresql://localhost:5432/Debts
    username: postgres
    password: bilal123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8080